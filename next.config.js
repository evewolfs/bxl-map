/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/bxl-map",
  // assetPrefix: "/bxl-map",
  reactStrictMode: true,
  images: { unoptimized: true }
};

module.exports = nextConfig;
